# illustrate R format add operator
li $t1, 100		#li stands for Load Immediate and is a convenient way of loading an immediate up to 32 bits in size.
li $t2, 50		# in easy word loding a  constant valu
add $t0, $t1, $t2

# illustrate add with an immediate. 
addi $t0, $t0, 50
add $t0, $t0, 50
